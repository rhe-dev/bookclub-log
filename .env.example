# 포트가 이미 사용 중이면 여기서 변경 (docker-compose가 자동으로 읽음)
FRONTEND_PORT=3000
BACKEND_PORT=4000
DB_PORT=5432

# PostgreSQL (로컬 개발용 기본값 — 그대로 사용해도 무방)
POSTGRES_USER=bookclub
POSTGRES_PASSWORD=bookclub
POSTGRES_DB=bookclub
