# 북클럽 로그 (BookClub Log) — 기획서

> 온라인 독서모임의 **책방(아카이브)** 서비스. 모임이 함께 읽은 책과 토론을 책 단위로 쌓고, 그 기록을 **모임 문집(실물 책)** 으로 만든다.
> 콘텐츠(모임의 책과 토론)가 본체이고, 책 제작은 그 위의 부가 기능이다.

---

## 1. 문제 정의

- 온라인 독서모임(오픈채팅 등)의 토론은 모임이 끝나면 **채팅방 스크롤에 흘러가 사라진다**. 좋은 코멘트가 책 단위로 모이지 않는다.
- 모임이 1년을 진행해도 "우리가 함께 읽고 나눈 것"이 남는 형태가 없다.
- 책을 좋아하는 사람들의 모임일수록 **실물로 남기고 싶은 욕구**가 크다. 흩어진 토론의 종착지로 '모임 문집'은 자연스러운 결말이다.

**기획 배경(실제 관찰)**: 사서인 지인이 오픈채팅으로 온라인 독서모임을 운영하는 것을 지켜봤다. 회원들은 감상과 의견을 부지런히 남기지만, 대화가 채팅 흐름에 묻혀 모임의 자산이 되지 못했다.

**인쇄 API 관점**: 모임 단위 주문은 개인 주문과 달리 **한 번에 여러 부(멤버 수만큼)** 가 나가는 구조라, 인쇄 API를 활용할 법한 콘텐츠 서비스의 전형적인 수요처다.

## 2. 타겟 사용자

**페르소나 A — 모임장 "지현"(30, 사서)**

- 오픈채팅 기반 5~6인 온라인 독서모임 운영. 월 1권.
- 책 선정·일정 공지·토론 진행을 챙기고, 모임의 기록을 정리해 남기고 싶어 한다.
- 책방 관리(책 등록·문집 제작)는 주로 데스크톱에서 차분히.

**페르소나 B — 회원 "민수"(28, 직장인)**

- 출퇴근길 모바일로 토론 참여. 인상 깊은 문장에 짧은 코멘트를 남기고, 다른 회원의 코멘트에 답글을 단다. (한 손, 1~2분)

**설계 함의**

- 토론(코멘트·답글)은 **모바일 우선** — 빠른 작성, 큰 터치 타겟.
- 책 등록·문집 제작은 **모임장 중심 + 데스크톱** 단계형 흐름.
- 회원 구분(누구의 코멘트인지·마이페이지)은 필수지만, 비밀번호 인증은 불필요 → **계정 선택만으로 로그인되는 목 로그인** (D-003·D-024).

## 3. 핵심 사용자 플로우

- **F0. 진입**: 서비스 소개 랜딩(`/`) → 로그인 모달에서 계정 선택(인증 없는 목 로그인) → 첫 가입 클럽 책방 진입. 여러 클럽 가입 회원은 GNB에서 클럽 전환 (D-024)
- **F1. 책 등록 (모임장)**: 책방 → `+ 책 추가` → 서지정보(제목·저자·출판사)·표지(색+이모지)·모임 날짜/기간·참여 회원 → 저장
- **F2. 토론**: 책 상세 → 코멘트 작성(선택: 페이지·인용 문장 지정) → 다른 회원이 답글·공감(좋아요) → 스레드로 쌓임. 내 코멘트는 수정·삭제 가능(삭제는 소프트 딜리트)
- **F3. 문집 주문**: 책방에서 책 선택 → `문집으로 내보내기` → 수록 내용 확인 → 문집 제목·부수 → 주문(주문자 = 현재 멤버) → **마이페이지에서 주문일·단계별 진행 날짜와 함께 상태 확인, 배송 완료 후 구매 확정**
- **F4. 주문 처리 (운영자)**: `/admin` → 주문 목록 → 단계 진행(주문 확인 → 제작 → 배송 → 배송 완료) → 날짜와 함께 이력 기록

## 4. 화면 목록 (IA)

| #   | 화면               | 핵심 요소                                                                 | 레벨 |
| --- | ------------------ | ------------------------------------------------------------------------- | ---- |
| 1   | 랜딩 + 로그인      | 서비스 소개(기능 3종)·모임 시작 CTA + 계정 선택 로그인 모달               | Lv1  |
| 2   | 책방 (모임 홈)     | 지금 읽는 책 + 지난 책 목록, 상태 필터, 책 추가, 빈 상태 온보딩           | Lv1  |
| 3   | 책 상세            | 서지정보·참여 회원·모임 일정 + 토론 스레드(코멘트/답글, 페이지·인용 앵커) | Lv1  |
| 4   | 문집 만들기        | 책 선택 → 수록 확인 → 제목·부수 → 주문 (단계형)                           | Lv2  |
| 5   | 마이페이지         | 프로필(클럽별 역할·로그아웃) + 내 주문(상태·스텝·이력·주문자 액션·사유) + 내 코멘트 모아보기          | Lv2  |
| 6   | 관리자 (/admin)    | 주문 테이블(주문자·주문일), 단계 진행, 날짜 이력, CSV 다운로드                                | Lv2  |

_(커뮤니티는 로드맵 보류 — §8)_

## 5. 데이터 모델 초안

```
Member             (id, publicId, name, avatarEmoji, color)            ← 전역 회원
Club               (id, publicId, name, description, inviteCode, createdAt)
ClubMember         (clubId, memberId, role: LEADER | MEMBER, joinedAt) ← 가입(다대다)·모임별 역할
Book               (id, publicId, clubId, title, author, publisher?,
                    coverColor, coverEmoji,
                    meetingDate?, periodFrom?, periodTo?,
                    status: UPCOMING | READING | DONE,
                    createdAt, updatedAt, deletedAt?)
BookParticipant    (bookId, memberId)
Comment            (id, publicId, bookId, memberId, parentId?, page?, quote?, content,
                    createdAt, updatedAt, deletedAt?)
CommentLike        (commentId, memberId, createdAt)  ← 공감(멤버당 1회 토글)
Order              (id, publicId, clubId, memberId(주문자), title, copies,
                    status: OrderStatus, createdAt)
OrderBook          (orderId, bookId)
OrderStatusHistory (id, orderId, fromStatus?, toStatus, changedAt, actor: USER | ADMIN,
                    reason?: OrderIssueReason, reasonDetail?)  ← 환불·재제작 요청 사유 (D-025)

OrderStatus — 순방향 8단계:
  RECEIVED(주문 접수) → CONFIRMED(주문 확인) → IN_PRODUCTION(문집 제작)
  → PRODUCED(제작 완료) → SHIPPED(배송 시작) → IN_TRANSIT(배송 중)
  → DELIVERED(배송 완료) → PURCHASE_CONFIRMED(구매 확정)
+ 분기 4 (D-013):
  CANCELED(취소)            ← 접수·확인에서만, 주문자·관리자
  REFUND_REQUESTED(환불 요청) ← 배송완료에서, 주문자 → REFUNDED(환불 완료, 관리자)
  REMAKE_REQUESTED(재제작 요청) ← 배송완료에서, 주문자 → 승인 시 IN_PRODUCTION 재진입(관리자)
  종결 상태: PURCHASE_CONFIRMED · CANCELED · REFUNDED
```

- **토론 스레드**: `parentId` 1단계(코멘트 → 답글). 페이지·인용 문장은 선택 입력 앵커. 코멘트는 작성자가 **수정(updatedAt)·삭제 가능**하며, 답글이 달린 코멘트를 지워도 스레드가 끊기지 않도록 '삭제된 코멘트'로 자리를 유지한다.
- **소프트 딜리트 원칙**: 삭제는 `deletedAt` 마킹을 기본으로 한다(Comment·Book). 토론 무결성과 문집 수록 이력을 보존하고, 하드 딜리트는 하지 않는다.
- **주문 상태**: 순방향 8단계 + **명시된 분기만** 허용(위 전이 맵), 그 외 역행·건너뛰기는 서버에서 거부. 단계 진행·환불 처리·재제작 승인은 운영자(ADMIN), **취소(제작 전)·구매 확정·환불/재제작 요청은 주문자(USER)만** 가능. 모든 전이는 날짜와 함께 이력으로 남겨 마이페이지·관리자에서 단계별 날짜를 확인할 수 있다. 문집은 주문 제작(POD) 상품이므로 단순 변심 환불·교환은 없고(전자상거래법 §17 청약철회 제한), 배송 후 하자 시에만 환불 또는 재제작 — POD에는 재고 교환이 없어 구제책이 '재제작'이다 (D-013).
- **식별자 전략**: 모든 노출 모델에 publicId(cuid, unique)를 두고 **API 경로·응답에는 publicId만 사용**. 내부 PK·FK는 자동증가 int로 유지해 조인 성능·디버깅 편의를 지킨다 (D-014).
- **배송은 상태로만 표현**: 주소 입력·택배 연동 등 실제 배송 기능은 구현하지 않는다(안내문 제외 대상). 실물 제작 서비스의 주문 라이프사이클을 상태 머신으로 반영하는 것이 목적.
- **회원-모임은 다대다**: Member는 전역 회원이고 소속·역할은 ClubMember(가입)로 관리한다(D-015). UI도 멀티 클럽을 반영 — 세션 = 멤버 + 현재 클럽, GNB에서 클럽 전환, 역할(모임장/멤버)은 클럽별 (D-024). 시드에 두 클럽·멀티 클럽 회원 포함.

## 6. 레벨별 범위와 완료 기준

### Lv1 — 모임 책방

- F0·F1·F2 + 화면 1~3.
- **완료 기준**: 시드 데이터만으로 즉시 탐색 가능 / 책 CRUD·코멘트/답글 동작 / 모바일 폭 확인.

### Lv2 — 문집 주문

- F3·F4 + 화면 4~6.
- **완료 기준**: 불법 전이·권한 없는 전이(구매 확정은 주문자만) 서버 거부(테스트로 증명) / 관리자 단계 진행이 마이페이지에 날짜와 함께 반영 / CSV 다운로드.

### Lv3 — 사용자 경험 설계

안내문 루브릭 4개에 1:1 대응:

1. **첫 진입 이해**: 랜딩(서비스 소개) → 계정 선택 로그인 → 책방까지 "무엇을 하는 서비스인지" 즉시 전달, 빈 책방 온보딩
2. **주문 상태를 사용자 말로**: "접수됐어요 → 문집을 만들고 있어요 → 배송 중이에요 → 배송이 완료됐어요" + 진행 스텝 (8단계는 접수·제작·배송·완료 그룹으로 묶어 표시)
3. **비정상 경로**: 빈 목록·로딩 스켈레톤·실패 재시도·입력 검증 메시지
4. **반응형**: 모바일(토론 참여 중심) ↔ 데스크톱(책방 관리·문집 제작 중심)

- **완료 기준**: docs/QA.md 체크리스트 전항목 통과 + 판단 근거가 README에 기록됨.

### 스트레치 (여유 시에만)

- 모임 만들기 + 초대코드 가입 플로우
- 표지 이미지 업로드

## 7. 의도적으로 만들지 않는 것 (비목표)

| 안 만드는 것                   | 이유                                                                                                                                                                                                     |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 비밀번호 로그인/회원 인증      | 과제 안내상 인증은 구현 대상 아님 + "실행 직후 바로 확인" 요건. **계정 선택 목 로그인**(D-024)으로 멤버 관점(작성자·마이페이지)은 유지하고 인증 비용만 제거. 제품 비전에는 실제 가입/로그인이 있으나 과제 범위에서 제외 |
| 커뮤니티(모임 간 소통)         | 코어(모임 책방) 완성이 우선 — §8 로드맵으로 보류                                                                                                                                                         |
| 결제·배송 기능(주소·택배 연동)·실물 제작·인쇄용 PDF | 과제 안내에서 명시적으로 제외. 배송은 주문 상태(라벨·날짜 이력)로만 표현                                                                                                                                                                            |
| 외부 도서 검색 API             | 외부 의존 배제 요건. 수동 등록 + 색/이모지 표지 — '우리 모임 책방' 감성과도 어울림                                                                                                                       |
| 런타임 AI 기능                 | API 키 필요 → docker-compose 단일 실행 요건과 충돌. AI는 개발 과정에서 활용                                                                                                                              |
| 알림·실시간(웹소켓)            | 토론은 재조회로 충분. 범위 절제                                                                                                                                                                          |

## 8. 로드맵 (과제 이후 디벨롭 — README "더 시간이 있었다면")

- **커뮤니티**: 모임 밖 회원들이 소통하는 공간 — 다른 모임의 책 후기, 모임 진행 노하우 공유, 원하는 모임 찾기
- 모임 만들기·초대코드 가입 정식 플로우
- 표지 이미지 업로드, 문집 구성 미리보기 고도화, 토론 알림

## 9. 기술 스택과 선택 이유

- **Frontend**: Next.js (App Router), TypeScript, TanStack Query, Zustand, Axios, MUI
- **Backend**: NestJS, Prisma, PostgreSQL
- **실행**: Docker Compose (db + backend + frontend), 포트는 환경변수로 변경 가능

**이유**: TypeScript 단일 언어로 프론트–백–DB를 잇는, 현재 실무에서 매일 사용 중인 조합. 제한된 기간에 학습 비용 없이 구현 완성도에 시간을 쓰기 위한 선택이다. 스키마→API→화면의 타입을 일관되게 유지할 수 있고, 서버 상태(TanStack Query)와 클라이언트 상태(Zustand)를 분리해 관리한다. UI는 검증된 컴포넌트(MUI)를 사용해 화면 판단(흐름·문구·상태 표시)에 집중한다.

## 10. 평가 포인트 매핑 (안내문 → 이 프로젝트)

| 안내문이 보는 것              | 이 프로젝트에서 확인되는 곳                                           |
| ----------------------------- | --------------------------------------------------------------------- |
| Docker 1회 실행 + 더미데이터  | docker-compose up → 시드 자동 실행 → 계정 선택만으로 즉시 탐색        |
| 서비스 플로우 완성 (Lv1)      | 입장 → 책방 → 책 상세 → 토론(코멘트·답글) 흐름                        |
| 비즈니스 로직 (Lv2)           | 주문 상태 머신(서버 전이 검증 + 이력) + 마이페이지 반영 + 관리자 화면 |
| 사용자 고려 설계 + 설명 (Lv3) | 루브릭 4종 구현 + DECISIONS.md → README 판단 기록                     |
| 바이브코딩 과정               | CLAUDE.md 라우팅 + HISTORY/TODO/AI_LOG — 문서 기반 workflow의 실증    |

## 11. 리스크와 대응

- **Docker 첫 사용** → 첫 주말에 빈 앱 3개 골격 기동까지 완료. 제출 전 클린 클론 테스트.
- **범위 팽창** → "Lv1 완성도 > Lv3 어설픔" 원칙. 새 아이디어는 TODO '아이디어(보류)'에 기록하고 멈춘다.
